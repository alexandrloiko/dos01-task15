1. created, restarting, running, paused, exited, dead

2. Server, Rest API, CLI

3. Можно ли потерять данные при выходе из контейнера, стоп его или удаления. Опишите варианты.

4. mount, volumes

5. Это файл в котором описываем, что необходимо игнорировать при сборке контейнера.

6. ENV позволяет устанавливать постоянные переменные среды, которые будут доступны в контейнерев во время его выполнения
ARG позволяет задать переменные для передачи Docker во время сборки образа, но переменные будут недоступны во время выполнения контейнера

7. Можно. docker-compose -f name_file.json up

8. Можно. docker-compose up -p name_project

9.Механизм применения метаданных к объектам Docker
LABEL multi.label1 = "значение1" \ 
      multi.label2 = "значение2" \ 
      other = "значение3"

